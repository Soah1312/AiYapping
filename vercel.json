{
  "git": {
    "deploymentEnabled": false
  },
  "ignoreCommand": "if [ -n \"$VERCEL_GIT_COMMIT_REF\" ]; then exit 0; else exit 1; fi",

  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
