rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Client-side Firestore access is disabled.
    // All Firestore operations are expected to happen through server routes/Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
